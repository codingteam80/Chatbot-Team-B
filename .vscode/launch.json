{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Test Chat",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/scripts/test_chat.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}