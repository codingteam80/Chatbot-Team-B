DocuBot v6.4.90 — Focused Generalization Retest
=================================================

RUN ONLY AFTER:
- v6.4.90 validator = PASS
- KB Health = PASS
- result ZIP has been reviewed

Use VS Code -> run.py and a fresh chat/session.

Affected-behavior checks:
1. Why does Rule 10.6 exist?
2. What is the rationale behind Rule 10.6?
3. What are the rules for compiler switch?
4. Which MISRA rules mention compiler command-line options?
5. List MISRA rules related to compiler flags.
6. What are the rules for switch statement?
7. ISO C Portability issue references?
8. Tell me about ISO C portability issue references.

Expected behavior:
- #1/#2: answer the actual source rationale by following the exact cited
  Section 8.10.3; do not stop at "see Section 8.10.3".
- #3/#4/#5: do NOT return Rules 16.1-16.7. Source-grounded compiler-option
  guidance should resolve to Rule 20.9 with a scope note.
- #6: still returns the C switch-statement Rule 16.1-16.7 family.
- #7/#8: source-grounded Section 6.10.1 answer; no unnecessary metadata dump.
  These should use the structured Section-title fast path rather than paying
  for vector/reranker/LLM work.

After these pass, perform the final broad/unseen robustness set. The project is
not considered limited to these prompts.
