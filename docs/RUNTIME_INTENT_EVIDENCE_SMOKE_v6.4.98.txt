DocuBot v6.4.98 — Runtime Intent + Evidence Selection Hardening
===============================================================

Base
----
Exact verified v6.4.97 Structured MultiQuery Output Hardening.

Scope
-----
This patch fixes downstream runtime intent/evidence behavior. It does NOT
replace the v6.4.97 structured MultiQuery design and does NOT change the
certified retrieval/model/chunking stack.

Focused recoveries
------------------
1. Goto-label scope -> Rule 15.3 is preserved and not displaced by broader goto rules.
2. Unused function parameters -> Rule 2.7; technical "allowed/permitted" no longer expands into authorization/role/access concepts.
3. Switch default omission -> direct No + Rule 16.4 instead of code-compliance "Needs more context".
4. Tagalog unused-parameter wording -> canonical source-language rerank intent + Rule 2.7.
5. New standalone side-effect question after recursion -> no stale prior-topic anchoring; Rule 13.5 rationale.
6. Standard Library pointer topic -> Rules 21.19 and 21.20 are retained for finalization.
7. String handling + pointer bounds -> exact concept intersection prefers Rule 21.17 before broad pointer-family rendering.
8. Near-threshold/exact/rescued Rule evidence is pinned through the final same-file diversity step.

Architecture lock
-----------------
- Generation model: qwen2.5:7b
- Embedding model: qwen3-embedding:8b
- Reranker: BAAI/bge-reranker-v2-m3
- Vector backend: Qdrant
- MultiQuery: original + 2 alternatives, strict JSON structured output from v6.4.97
- Fusion: RRF
- Vector/BM25/Final Top-K: 10/10/3
- MIN_RETRIEVAL_SCORE: 0.55
- Chunking: v4, 900/150
- Few-shot: enabled
- Silent grounded reasoning guidance: enabled
- Visible chain-of-thought requested: no
- KB rebuild: no

Focused unseen/manual retest
----------------------------
Use natural paraphrases, not only the exact examples below. At minimum verify:
- goto label scope
- unused function parameter permission
- switch default-label omission
- Tagalog unused parameter
- RHS persistent side effects with &&/||, including a turn immediately after a recursion question
- pointer returned by Standard Library functions
- string handling + pointer bounds

Do not change MIN_RETRIEVAL_SCORE to make these pass. The patch is intended to
fix intent routing, rerank canonicalization, evidence preservation, and
specificity selection while keeping the global threshold unchanged.
