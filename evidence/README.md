# Evidence Directory

This directory contains retained certification/release-history material. It is deliberately separate from the active runtime architecture.

The high-volume test, chunking, optimization, and latency evidence remains under `logs/` so earlier measured results are still inspectable. Nothing in `evidence/` or historical `logs/` is required as an active model/retrieval component.
